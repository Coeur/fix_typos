<% require "YAML" %>
<% @data = YAML::load(File.open("repos.yml")) %>

# fix_typos
A Campaign that has the target to run the https://github.com/client9/misspell spelling checker on as many - but not only - iOS related repositories

Want to have a pull request for your repo? Just add it to the list!

<%= "## Todos" %>
<% for @repos in @data["todo"] %><%= "* [#{@repos["name"]}](https://github.com/#{@repos["name"]})" %>
<% end %>

<%= "## Open" %>
<% for @repos in @data["open"] %><%= "* [![Pull Request for #{@repos["name"]}](https://github-shields.com/github/#{@repos["name"]}/pull/#{@repos["pr"]}.svg)](https://github.com/#{@repos["name"]}/pull/#{@repos["pr"]})" %>
<% end %>

<%= "## Merged" %>
<% for @repos in @data["merged"] %><%= "* [![Pull Request for #{@repos["name"]}](https://github-shields.com/github/#{@repos["name"]}/pull/#{@repos["pr"]}.svg)](https://github.com/#{@repos["name"]}/pull/#{@repos["pr"]})" %>
<% end %>

<%= "## Nothing to do" %>
<% for @repos in @data["notodo"] %><%= "* [#{@repos["name"]}](https://github.com/#{@repos["name"]})" %>
<% end %>
